services:
  web:
    container_name: todolist-web
    build:
      context: ./source
      dockerfile: deploy/todolist/Dockerfile
    env_file: .env
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
